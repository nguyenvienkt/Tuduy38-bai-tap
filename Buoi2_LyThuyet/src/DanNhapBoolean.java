
public class DanNhapBoolean {

	public static void main(String[] args) {
		// Boolean chỉ chứa được 2 giá trị [true] hoặc [false]
		/* Các phép so sánh 
		 * < (nhỏ hơn), 
		 * > (lớn hơn), 
		 * <= (nhỏ hơn hoặc bằng), 
		 * >= (lớn hơn hoặc bằng), 
		 * == (bằng), 
		 * != (khác)
		 * A&&B --> phép và (&&) đúng khi cả 2 cùng đúng
		 * A||B --> phép hoặc (||) đúng khi một trong 2 đúng
		 * !A --> phép phủ định, ví dụ A là true thì phủ định là false, ngược lại A là false thì phủ định là true
		 * */
		int 	a 		= 3;
		int 	b 		= 4;
		//boolean value = a+b;
		
		//System.out.println(value);

	}

}
